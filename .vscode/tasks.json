{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docs",
            "type": "shell",
            "command": "java -jar swagger-codegen-cli-2.2.1.jar generate -l html2 -i rfnet_link_api.json -o docs",
            "problemMatcher": []
        }
    ]
}