version: '3.2'
services:
  sign:
    build:
      context: .
      dockerfile: ./dockerfile-sign
  web:
    build:
      context: .
      dockerfile: ./dockerfile-phx
    volumes:
      - type: bind
        source: ./rfnet_hub
        target: /app
    ports:
      - "4000:4000"
    depends_on:
      - db
      - sign
  db:
    image: postgres